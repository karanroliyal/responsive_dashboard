export const environment = {
    production: false,
    base_url: `${window.location.protocol}//${window.location.hostname}:4200/`,
    server_url: `${window.location.protocol}//${window.location.hostname}/`
  }; 